version: 1
filesystem:
  workdir: true
  read_only: []
  read_write: ["/workspace", "/tmp"]
network:
  mode: allowlist
  endpoints:
    - { host: api.github.com, port: 443 }
resources:
  cpus: 1
  memory: 1G
